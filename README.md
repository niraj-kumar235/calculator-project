# calculator-project
# 🧮 Calculator Web App

A simple and responsive **Calculator Web Application** built using **HTML, CSS, and JavaScript**.
This project performs basic arithmetic operations with a clean UI and smooth user interaction.

---

## 🚀 Features

* Basic operations: ➕ Addition, ➖ Subtraction, ✖️ Multiplication, ➗ Division
* Percentage (%) calculation
* Clear All (AC) functionality
* Delete (DEL) last input
* Responsive and minimal UI design
* Real-time input display

---

## 📁 Project Structure

```
calculator-project/
│── index.html   # Main HTML structure
│── style.css    # Styling (UI Design)
│── script.js    # Calculator logic
```

---

## 🛠️ Technologies Used

* HTML5
* CSS3
* JavaScript (Vanilla JS)

---

## 📸 Preview

The calculator interface includes:

* Display screen for input/output
* Buttons for numbers and operators
* Clean grid layout design

---

## ⚙️ How It Works

* User clicks buttons → value gets appended
* Special buttons:

  * `AC` → clears all input
  * `DEL` → removes last character
  * `%` → converts value into percentage
  * `=` → evaluates the expression

The calculation logic is handled using JavaScript's `eval()` function.

---

## 💻 Installation & Usage

1. Clone the repository:

```bash
git clone https://github.com/your-username/calculator-project.git
```

2. Open the project folder

3. Run the project:

* Open `index.html` in your browser

---

## 📌 Code Reference

* HTML structure: 
* JavaScript logic: 
* Styling (CSS): 

---

## ⚠️ Note

* This project uses `eval()` for calculation, which is not recommended for production-level applications due to security concerns.
* For advanced apps, consider safer parsing methods.

---

## 📈 Future Improvements

* Scientific calculator functions
* Keyboard input support
* Dark mode
* History feature

---

## 🙌 Author

* Your Name

---

## ⭐ Support

If you like this project, give it a ⭐ on GitHub!
